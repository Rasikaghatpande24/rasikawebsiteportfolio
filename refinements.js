(()=>{const hero=document.querySelector('.portrait-scroll');if(!hero)return;const stage=hero.querySelector('.portrait-stage'),title=hero.querySelector('.portrait-title'),portrait=hero.querySelector('.portrait-image'),script=hero.querySelector('.portrait-script'),progress=hero.querySelector('.portrait-progress i');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let queued=false;function update(){queued=false;if(reduced)return;const r=hero.getBoundingClientRect(),p=Math.max(0,Math.min(1,-r.top/Math.max(1,hero.offsetHeight-stage.offsetHeight)));title.style.transform=`translateY(${-p*65}px) scale(${1+p*.13})`;portrait.style.transform=`translateX(-50%) translateY(${p*35}px) scale(${1+p*.045})`;script.style.transform=`translateX(-50%) translateY(${-p*35}px) rotate(${-9+p*5}deg)`;progress.style.width=p*100+'%'}addEventListener('scroll',()=>{if(!queued){queued=true;requestAnimationFrame(update)}},{passive:true});addEventListener('resize',update);update();const dialog=document.getElementById('contact-dialog');document.querySelectorAll('a[href="#contact"]').forEach(a=>a.addEventListener('click',e=>{if(dialog){e.preventDefault();dialog.showModal()}}));if(dialog){dialog.querySelector('.close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}})}document.querySelectorAll('.whatsapp-pending').forEach(b=>b.addEventListener('click',()=>{if(!dialog.open)dialog.showModal();dialog.querySelector('.whatsapp-note').hidden=false}));})();